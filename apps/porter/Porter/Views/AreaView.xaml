<UserControl x:Class="Porter.Views.AreaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="36">
        <StackPanel DockPanel.Dock="Top">
            <TextBlock FontSize="22" FontWeight="SemiBold" Text="Pick the configuration area"/>
            <TextBlock Foreground="Gray" Margin="0,4,0,14"
                       Text="Each card shows the area's real SWIS mechanism, verified against the 2026.2 contract. Greyed areas are pinned to a later iteration — the card says why."/>
        </StackPanel>
        <Button DockPanel.Dock="Bottom" Content="Back" HorizontalAlignment="Left"
                Margin="0,14,0,0" Click="Back_Click"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="Cards">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
